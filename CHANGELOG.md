# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与 [SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed
- **日/周/月线改按年分区**（分钟级维持按日）。原来一律"每个交易日一个文件"，
  对日线就是**一行一个文件**：全库 17,059 个文件、52.3 MB，而全部分钟线才 1,836 个、11.5 MB。
  合并后 **345 个文件、1.7 MB**，九宫格里日线一格从 **770ms 降到 11ms**，
  八格合计 1271ms → 384ms。这是 Parquet 生态经典的 small files problem
  （Delta Lake 的 OPTIMIZE / Iceberg 的 rewrite_data_files 解决的是同一件事）。
  代价是写放大：每天收一根日线要重写整年（实测 2.2ms，66 标的 × 3 周期每天多 0.4 秒）。
  存量数据用 `scripts/compact.py` 迁移 —— **先校验逐根一致再删**，幂等、可中断重跑。
- **`read_range` 先按文件名裁分区再读**（partition pruning），并按 close_ts 去重。
  有界区间实测 137ms → 10ms。去重是迁移期兜底：新旧布局并存时同一根 bar 会被读两遍。
- **`/api/markers` 改用列裁剪**：它只需要"有哪些 bar"来给信号定位（只用 `close_ts`），
  却在读全部 10 列再造几万个 `Bar` 对象。Parquet 是列存，只读要用的那一列
  （projection pushdown）—— BTC 1m 三万根从 152.7ms 降到 12.8ms。
  八个周期的 markers 合计 **330ms → 96ms**，九宫格一次刷新 500ms → 280ms（服务端）。
  16 个响应逐字节对拍无差异 —— `dropped` 的判定只依赖 close_ts 集合，语义完全不变。
  （这里**不能改尾读**：窗口外的老信号会被误判成 dropped。）
- **面板不再读全量再截尾**：无界区间走 `read_tail`（从最新分区往回读、够了就停），
  行数由 `count_bars` 从 Parquet 元数据拿。均线的前置预热由 `warmup_bars` 算 ——
  SMA 取窗口、EMA 实测要 20 倍窗口。九宫格八格 **385ms → 74ms**（最初 1271ms），
  1m 一格 226ms → 12ms，分时 205ms → 32ms。
  改前改后 40 个接口响应逐字节对拍：bars / total / 预热位置全同，
  均线最大相对误差 1.4e-15（滚动累加的浮点噪声，非口径差异）。

### Added
- **质量统计新增五个分析区块**（左栏下方原本空着一半）：
  **超额收益**（规则期望 / 随机进场基准 / 超额 + 95% 区间条，**跨零时一律中性灰**
  并给出「样本分辨不出优势」的警示 —— 颜色本身编码结论站不站得住）、
  **权益曲线 + 回撤**、**浮盈/浮亏散点**（止盈线水平、止损线垂直）、
  **收益分布**、**持有期敏感性**（超额与毛期望两条线，竖线标当前口径）。
  超额此前只在 `scripts/rule_eval.py` 里，面板一个字都没有 ——
  而 CLAUDE.md 定的判据就是超额不是毛收益。设计稿见 `docs/design/stats-blocks/`。
- 持有期曲线**同时给毛期望和基准**：基准本身也随持有期变
  （实测从 +0.0002% 涨到 +0.035%），只画毛期望会把「市场在涨」
  误读成「规则在长持有期上更好」。

### Fixed
- **`evaluate_all` 是 O(信号数 × bar 数)**：它对每条信号都重扫整条序列，
  而 `evaluate` 只用得到 `future[:horizon_bars]`。接上基准计算后
  `/api/stats` 一度要 6.3 秒。改成按 close_ts 二分定位再切一小段：
  **算基准 5795ms → 133ms（44 倍），结果逐位不变**；`/api/stats` 6269ms → 1005ms
  （含新增的持有期扫描）。试算、rule_eval、纸上撮合走同一函数，一并提速。
- **信号流补上时间筛选**（今天 / 近 3·7·30 天 / 指定日期）。上一版只做了翻页，
  而当初说的是「按日期翻页」—— 服务端 `since`/`until` 早就支持，缺的是界面入口。
  **走服务端筛选**（不是只筛已加载的那批），所以能直接跳到任意日期。
- **统计口径栏的控件高矮不齐**：`.chip` 里装数字框/下拉框/复选框/日期框，
  四者默认行高不同，实测极差 29.5px。给 `.chip` 钉 `min-height`，
  并把 `select`/`date` 一起纳入「去掉自身边框」的覆盖（原来只覆盖了 `input`）。
- **「信号流」标题被挤成竖排**（加第三个筛选框之后）。与「九宫格」「cell-head」
  同一个成因，这是第三次 —— 标题 `flex:none` + `nowrap`，让控件换行。
- **breakout 的 `box` 用固定百分比，事实上不适用于高波动品种。** 本项目其余阈值
  都 ATR 归一，只有这条是例外：`consolidation(20, 0.008)` 在铜上 49.8% 的时间成立、
  在股指上只有 **0.3%**，各品种命中率极差 **170 倍** —— 规则声称 `universe: CN.*`
  覆盖全品种，实际不是。用户在 UR701 上踩到（box 0/123），排查一轮才发现
  不是「最近没横盘」。新增 `range_atr(n)` 原语（返回振幅是几倍 ATR），
  改为 `range_atr(20) < 3.83`：极差降到 2.7 倍，股指从 0.3% 回到 25.7%。
  3.83 是**与原阈值等量**的点（总命中率同为 26.9%），改动纯粹是重新分配、不放量。
  ⚠️ **收益上没测出好处**（阈值扫描非单调、全在噪声里），改它的理由是语义正确与全品种公平。

### Added
- **质量统计加时间范围**（全部 / 近 30·90·180 天 / 近一年 / 自定义）。
  **时间范围是口径的一部分** —— 信号只增不减（没有清理策略），不限区间的话
  两年前的行情会和现在混进同一个期望值里，随数据变多越来越失真。
  区间跟着报告原样带回并显示在口径行里：不写明区间的胜率没有意义。
  日期按 **CST** 解释（期货交易日就是 CST 的日历日），不随机器时区变。
- **信号流可以往回翻页**（每页 500）。`offset` 从**新往旧**数 ——
  从旧往新数的话新信号一到达锚点就错位，翻着翻着会重复或跳过。
  往回翻是追加不是替换（图上的标注也按 S.signals 画），按 dedup_key 去重。
  条数改显示「筛出 N 条 / 已加载 M · 共 T」：只显示筛出条数的话，
  会以为库里就这么多。`since`/`until` 在 SQL 里过滤，不是取回来再筛。
- **Windows 双击启动**：根目录加 `启动面板.bat` / `更新程序.bat`，不用再记命令。
  两者都会先自检（虚拟环境、端口占用、是否 git 仓库、有无未提交改动），
  出错时停住并说明怎么办，不会一闪而过。
  **不做成 exe**：体积 150–300MB、杀毒误报常态、每次改代码都要重打包
  （git 更新流程就废了），而且对性能毫无帮助 —— 实测切标的 677ms 里
  浏览器渲染只占 59ms，其余是服务端往返，exe 打的是同一个 CPython。
  配 `tests/test_launchers.py` 静态守住 CRLF/无 BOM/每条退出路径都 pause 等
  「双击后窗口一闪而过」的坑（开发机是 Linux，跑不了 cmd.exe）。
- **键盘快捷键**（盯盘时手不用离开键盘）。取的是国内期货软件的手感：
  `1`–`9` 按周期条从左到右切周期、`PgUp`/`PgDn` 翻标的、`Esc` 逐层回退；
  另有 `Alt+M/H/D/W` 字母助记、`J`/`K` 翻信号流、`G` 开关九宫格、`T` 换网格模式、
  `[`/`]` 切预警组市场。**输入框里一律不响应**（规则编辑页有 textarea），
  也不碰浏览器占用的 `Ctrl+数字`/`Ctrl+W`/`F5`。
  `?` 或头部的「⌨」按钮弹出快捷键表 —— **快捷键不可见就等于不存在**，
  所以这个入口和快捷键本身是同一件事的两半。

### Fixed
- **快捷键「⌨」按钮从图表控件区移到顶栏**。它是全局功能，跟「提醒方式」同级，
  不是某张图的控件；放在「九宫格」旁边既是信息架构错位，也把那个按钮挤到过换行。
  样式改为复用顶栏的 `.opt`，与相邻按钮同组。
- **`os.kill(pid, 0)` 在 Windows 上会终止目标进程**（严重）。Python 的 `os.kill`
  在 Windows 上没有「信号 0 探测」：除 CTRL_C_EVENT/CTRL_BREAK_EVENT 外走的是
  `TerminateProcess()`。这是**生产路径** —— `watch.py` 启动时 `claim_writer` 会调它，
  库里若留着陈旧的写者记录、那个 pid 又被系统回收给了别的进程，启动盯盘就会杀掉一个
  无关进程。改为 Windows 走 `OpenProcess` + `GetExitCodeProcess`（只读不写）。
  用户在 Windows + Python 3.14 上报「测试挂起」，查下去是这个。
- **测试会读到开发机上真实的 `~/.signal-desk/.env`**。断言「没找到 .env」的用例
  在**配过凭据的机器上必然失败**（用户的 Windows 机器就是这么红的，代码本身没问题），
  更糟的是**跑测试会把真实凭据灌进 `os.environ`**。
  `conftest.py` 加 autouse 隔离，把 `USER_ENV` 指到临时目录并清掉 `SIGDESK_ENV`。
- **右上角时钟显示的是 UTC**，比国内墙上钟慢 8 小时。而面板上期货的每个时间戳
  都是 CST —— 全屏就这一个表跟别人不一致。改为走市场时间（CST，UTC+8 硬算，
  与 chartTime/fmtTime 同口径，不依赖本机时区设置），UTC 放进 title。
- **预警组的钉住按钮完全没用**，而且两条分支各错各的、**都是静默失败**：
  钉住把 path 传成了 `send()` 的 method（fetch 拿到非法动词，请求根本没离开浏览器），
  取消钉住给只读的 `api()` 传了个它压根不看的 `{method:"DELETE"}`（于是发成 GET，撞 405）。
  两条都不进控制台，按钮看着就是"点了没反应"。
  根因是两个 helper 长得像、签名不一样（`api(path)` vs `send(method, path, body)`）——
  现在 `api()` 多收参数当场抛，并有测试把两者的所有调用点过一遍。
  钉住的点击靶子同时从 11×11 放大到 24×24（图标仍是 11px）。
- **「九宫格」按钮被压成竖排**（这次是三行）。上一轮把均线图例浮起来只是给它腾了地方，
  没治根 —— 根因是它在 flex 头部里可被压缩。`#grid-toggle` 补 `flex:none` + `nowrap`。
- **跨零点夜盘的凌晨段，跨周末时交易日归错**（`MarketCalendar.trading_day`）：
  周五夜盘 23:59 归下周一、同一场的周六 00:00 却归周六 —— **一场夜盘被劈成两个交易日**。
  后果是日线聚合抛「时间倒流」、Parquet 分到两个分区；接上 au2610（夜盘到 02:30）后
  `watch.py` 直接崩在启动阶段。`in_session` 一直判对了，只有 `trading_day` 没跟上 ——
  两个本该同源的判定各写各的。**修完历史数据必须重补。**
- **`backfill.py` 跨度大就读超时**（实测两个月必挂、一周稳过），且超时抛在流读取阶段、
  看起来像"连不上"。改为内部按周分段拉（幂等）。
- **期货历史按标的降级**：5 个品种里 1 个拉失败，原先整个期货市场被跳过。
  现在只丢拉不动的那个。
- 均线图例塞进 `chart-head` 会把标的选择器和周期按钮挤到换行（「九宫格」被压成竖排）。
  价均线与量均线的图例**都浮在图左上角**（看盘软件的惯例位置）。这个坑踩了两次。
- **任一行情源连不上就整体启动失败**：加密挂了连能用的期货也盯不了。
  改为**按市场降级** —— 一边失败只警告并跳过那一边，两边都挂才算真的起不来
  （并说明原因与排查方向）。缺凭据单独提示缺哪个键、怎么配，不再抛 `KeyError`。
  用 hosts 污染 OKX 与抽掉凭据两种方式实跑验证过。
- `.env.example` 里残留真实行情主机名，已换成占位符（测试钉住不再回归）。
- **下拉框里有、却从没被采集过的标的**：面板列全部注册标的（为了能看主连），
  但 `watch.py` 只采集**出现在某条规则 `universe` 里**的标的。
  于是 `IF2609/m2701/cu2610` 在下拉框里存在、选中却是一张空图 ——
  用户只会以为"期货连不上"（真发生过）。现在：
  - `/api/meta` 每个标的带 `watched`，下拉框标出「**· 未盯**」；
  - 空状态按"有没有人盯"分开说明，并给出下一步（加进规则 universe，或 backfill 回补）。
- 图表空状态里的 `<b>`/`<br>` 被 flex 容器拆成多个 flex item 排成一行，换行全乱。
  内容裹进一层块级元素即可（截图当场看到）。
- **九宫格单击就放大**，导致没法停在小格里看十字线对比多周期。改为**双击**放大/还原。
- **日/周/月格子里的信号箭头被撑到半个格子**：lightweight-charts 的标注尺寸随 bar
  宽度缩放，只有一两根 bar 时每根极宽。少于 20 根就不画标注 ——
  盖住 K 线不说，还让人误以为那是个特别重要的信号。
- **`#grid-toggle` 按钮没插进 index.html**（字符串替换缩进不匹配，静默失败），
  真实浏览器一开就白屏 —— 而**桩化冒烟照样绿**：DOM 桩对任意选择器都返回节点，
  HTML 里少一个元素它完全看不出来。现在冒烟会把 app.js 查过的 `#id`
  与 index.html 对一遍（`missing_ids`），并验证过"删掉按钮它确实会红"。
- 九宫格格子标题曾用 `.tf` 类名，与顶部周期按钮撞名 —— 选择器互相误伤，
  截图脚本点 `.tf` 直接超时。改为 `.cell-tf`。
- 小格子里 TradingView 角标正好压在 K 线上（9 格就是 9 个），
  用 v4.2 的 `layout.attributionLogo: false` 关掉（单图页保留，那里不碍事）。
- 「点击放大/还原」提示与格子标题都绝对定位在右上角，叠成乱码。提示挪到左下。
- **`neutral` 方向的信号被画成向上箭头**（灰色）：会被读成"看多"。改为灰色圆点。
  多=绿上箭在 bar 下方／空=红下箭在 bar 上方，三者一眼分得开。
- **成交价完全没画到图上**：`/api/markers` 只返回信号，纸上撮合的开仓/止损/止盈价
  一条都看不到。现在信号与成交**分开画**，成交点带价格文字；选中信号时把触发价与
  对应成交价画成横线 —— 箭头只说明"哪一根"，横线才说明"哪个价"。
  成交沿用与信号**同一套服务端分桶**，避免"箭头在这根、成交在那根"。
- **新建的规则在盯盘页的信号流筛选框里找不到**：`#f-rule` 只在 `boot()` 填一次，
  保存规则后 `S.meta` 没更新，要刷新整个页面才看得到。抽出 `fillRuleFilter()`，
  保存/删除后重拉 meta 重填，且重填不冲掉用户当前选中的筛选项。
- **规则编辑器的文字是纯黑，深色底上看不见**（对比度 1.17:1）：基础表单样式
  `select,input,button{color:var(--fg)}` **漏了 `textarea`**，而表单控件不继承
  页面的 `color`。唯一显式设过颜色的是 `#rule-src:disabled` ——
  **截图恰好只截到那个禁用态，所以一直没被发现**。
  同时给 `::placeholder` 显式上色（各浏览器默认不一，Firefox 还带 opacity）。
- `shoot_panel.mjs` 新增**对比度检查**（阈值 3:1，只抓"基本看不见"，
  不管有意做淡的次要文字），并补截「新建规则」那一屏 ——
  编辑器**启用态**的文字颜色只有在那个状态才看得到。
  撤掉修复后该检查确实报错并以非 0 退出，验证过。
- **`watch.py` 只派生规则用到的周期，导致图表周期静默停更**（今天引入的回归）：
  它同时喂两个东西 —— 规则引擎（只要规则用到的）和面板图表（要全部可选周期）。
  规则只用 15m 时，5m/30m/1h/4h 全部不再更新，**面板上看着就是「行情连不上了」**。
  改为 `DEFAULT_TIMEFRAMES ∪ store_timeframes(rules)`，与 `paper_run`/`replay_check` 一致
  （那两个当时就用了并集，唯独漏了最该用的这个）。
- **`watch.py` 不加 `--web` 时行情完全不落盘**：盯盘进程跑得好好的，面板却一直是
  几天前的数据。改为**一律落盘** —— 盯盘却丢掉自己收到的行情，本身就说不通。
- 图表页脚补上「末根多久以前」。只陈述事实不判断对错（判断"该不该有新数据"要看日历），
  但「你正在看三天前的数据」必须一眼可见 —— 进程没在跑时图看着完全正常、只是永远不动。
- **`/api/rules/{id}` DELETE 返回平台相关的路径分隔符**：Windows 上是
  `rules\\_trash\\x.yaml`，任何按路径断言或展示的调用方跨平台就炸
  （Windows 验收时当场挂了一条测试）。改为一律 `as_posix()` —— **JSON API 的
  路径表示是接口约定，不该跟着操作系统变**。
- **验收脚本把「环境缺东西」记成失败**：那台 Windows 机器没装 playwright，
  截图组报了 2 条假红。现在区分 **❌ 失败 / ⏭️ 跳过**，退出码只看失败项，
  跳过项单独列出并写清怎么补齐。否则每台新机器都冒假红，真缺陷会淹没在里面。
- **`run_acceptance.bat` 的两个可用性问题**（Windows 验收暴露）：
  `pause` 在 agent/CI 的非交互 shell 里永远挂住（改为默认不 pause，
  双击场景用 `--pause`）；`python` 常指向 3.11 不满足 `>=3.12`
  （改为依次试 `py -3.14/-3.13/-3.12/-3`、`python3`、`python`，找不到明确报错）。
  日志落盘挪进 `acceptance.py --log`：**cmd 的管道会让 `%ERRORLEVEL%` 变成管道
  最后一个命令的返回值**，用管道 tee 会把退出码丢掉。
- **`types-PyYAML` 从未写进 `pyproject.toml`**：我的开发环境手装过，所以本地
  mypy 一直是绿的；**干净环境里会报 4 个 import-untyped 错**。打测试包、解压到
  空目录重跑才发现 —— 「本地能过」不等于「别人能过」。
- **回补脚本与面板的落盘路径不一致**：`backfill.py`/`build_continuous.py` 写 `data/bars/`，
  而 `watch.py`/`serve.py`/`report.py`/`paper_run.py` 读写 `data/` —— **回补的数据面板
  一根都读不到**。六个脚本统一到 `data/bars/`，已有的 `data/CRYPTO/` 一并迁入。
- **图表空状态被 lightweight-charts 的 canvas 完全盖住**：元素在、文字在、visibility
  正常，屏幕上却一片漆黑，看着像加载失败（canvas 是 z-index:1/2 的绝对定位层）。
  截图看得见症状但说不出原因，靠 `elementFromPoint` 命中测试才定位到。
  `shoot_panel.mjs` 现在每张截图后都跑一遍**遮挡检查**，把这条变成常驻不变量。
- **换标的/换周期后可视区间不重置**：从 259 根的 5m 切到 61 根的日线，
  旧区间会让新数据缩在右边、左边空掉三分之一。没有可定位信号时显式 `fitContent()`。
- **主连选不到**：`/api/meta` 用的是 `registry.tradable()`，它排除主连 ——
  于是自己拼出来的连续序列在面板下拉框里根本不存在。改为列全部标的并标出「主连」；
  排除主连是「预警/下单」路径的事（`tradable()` 仍排除），不是看图的事。
- **函数参数个数不在编译期检查**：`ema(close, 60, '1h')` 能编译通过，要等**上线后
  第一根 bar** 才抛 TypeError —— 而 `config/rules/` 是 fail-fast 加载的，坏规则本不该
  进得了生产。现在 `FuncSpec` 带 `inspect.signature`，编译期就核对形状。
  同时拒绝 `f(**d)`（其 `keyword.arg` 是 None，求值器会静默丢掉，参数凭空消失）。
- **面板把"算不出来"显示成 0**：0 条可评价信号时，质量统计页与规则试算都会显示
  「胜率 0.0%」「期望收益 +0.000%」—— 那不是不赚钱，是没有样本。现统一显示破折号。
  与 ADR-0006「预热期是 None 不是 0」同一条原则（M4 已在收益率上栽过一次）。
- **`[hidden]` 被 `.badge{display:flex}` 盖掉**（类选择器优先级高于 UA 样式表）：
  「未保存」徽标一打开规则就亮着。补 `[hidden]{display:none!important}`。**截图才看得出来。**
- **`confirm_on_close: false` 静默无效**：规则里能写、加载器也认、`Signal.tentative`
  字段与落盘/推送/面板分支全都在，**唯独没有任何一处会把它置为 True** —— 用户以为
  开了盘中预报，实际还是只在收盘报。盘中预报实现之前，加载器直接拒绝启动。
  同时要求它必须是真布尔值（`bool("false")` 是 `True`，用 `bool()` 兜是第二个陷阱）。
- **`MarketCalendar.in_session()` 只比对时刻、完全不看日期** —— 周六上午十点与国庆节
  上午十点都被判成「在交易时段内」。后果：健康面板每个周末把期货全报成「数据滞后」，
  `PollingFeed` 整个周末空转。现在按 自然日 → 周末/节假日 → 夜盘归属 三级判定。
- **节假日表被 YAML 静默吞掉**：PyYAML 把不带引号的 `2026-01-01` 解析成 `datetime.date`
  对象，而内部按 ISO 字符串比对，`frozenset` 永远匹配不上 —— 假期表写了等于没写，
  且不报任何错。`MarketCalendar.from_config` 现在统一归一（date/datetime/str 都接受）。
  与 M2 那个裸键 `on:` 被解析成布尔 `True` 是同一类坑。

### Fixed
- **网格模式下切换标的，左上角标题不跟随**（一直停在上一个）：标题只在 `loadChart`
  里设，而网格走的是 `loadGrid` / `loadWatch`，根本不经过它。收成 `setChartTitle()`，
  凡是换了"现在在看什么"的地方都调它。顺带修了下拉框 onchange 直接调 `loadGrid`
  的问题 —— 网格有两种模式，那会在预警组模式下把周期格子塞进标的格子的网格里。
- **加密的日/周/月线只有几根**：与期货同因（高周期靠 1m 聚合，1m 只回补了三周），
  但加密走 OKX 不走行情 API，`backfill.py` 用不了。现在按市场分派，
  加密同一条命令即可（走公开接口、不需要凭据）。实测 BTC 日线 23 → 998 根。
  `normalize_candles` 相应放行 `1D`：**加密的"一天"就是 24 小时 UTC**，
  而 `Timeframe.D1` 在模型里是日历周期（国内期货一个交易日含前一晚夜盘），
  `seconds` 为 0，不补长度就会报"不是固定长度周期"。周线月线仍然拒绝（真的不定长）。
- **5m 图上的日线均线看不见**：BTC 只有 23 根日线，SMA20 要 20 根预热，
  只有最右端一小段有值。补完日线历史后全程覆盖。
- **跨周期均线锯齿感强**：改画成明确的阶梯线（`LineType.WithSteps`）。
  它本来就是台阶，用普通折线画时每次跳变会连出一段陡斜线，缩小看像"想画平滑却在抖"。
  **没有改成平滑曲线** —— 那等于在两次收盘之间显示一个当时还不知道的中间值。

### Changed
- **只保留「看大做小」与「突破」两类规则**，删掉 `bounce-*` / `ema-cross-long` /
  `volume-spike`。指向被删规则的注释一并改掉（量能均线窗口 20 的口径来源改为
  breakout 的扳机）；测试不再点名某条出厂规则 —— 点名会在删规则时误伤，
  让人以为删坏了东西。
- **universe 支持通配符**：`CN.*` = 国内期货全品种，**在加载时展开**成具体 uid。
  `Rule.universe` 有十几处消费方（引擎、试算、面板、采集列表、纸上回测…），
  让下游各自解析通配符的话，任何一处漏了都表现为"这个标的悄悄不被盯" ——
  没有报错，只是永远收不到那个品种的信号。展开一次，下游拿到的永远是具体清单。
  主连自动排除（不可下单，不该预警）。没有 registry 就报错，不静默当成空 universe。
  往 `config/symbols.yaml` 加合约即自动纳入，规则文件不用动。
  加密仍只盯 BTC / ETH：OKX 上几百个永续绝大多数流动性差，而 ATR 归一阈值
  假设了正常深度。
- 跨周期均线**只在 5m 上叠**：1h 图上再叠 1h 是重复，1d 图上叠 1h 是噪音，
  1m 图上那两条台阶会横穿整屏。

### Added
- **跨周期均线叠加**：`/api/bars?ref_ma=1h:ema20,1d:sma20`，在 5m 图上叠 1h / 1d 均线
  （画得更粗，与本级别均线分得开）。**严格 as-of 对齐**：一根 1h 均线的值只有在
  那根 1h 收盘之后才可见，图上呈现为台阶而不是插值 —— 摊到那一小时的每根 5m 上
  等于让 09:00 看到 09:59 的数据，复盘会得出"当时明明看得出来"的错误结论。
  与 INV-1 是同一条纪律。

### Changed
- **规则按「超额」重新评估，`kdzx-long` 关闭、`kdzx-short` 重做。**

  样本区间等权 **+10.05%**（加密 +21%/+27%），不减基准的话所有多头规则都显得好、
  所有空头都显得差。改用**超额** = 期望 − 随机进场的期望（基准按各规则自己的
  信号分布加权），排序完全反转：

  | 规则 | 条数 | 毛期望 | 超额(1bp) |
  |---|---:|---:|---:|
  | breakout-short | 75 | +0.011% | **+0.049%** |
  | kdzx-short（重做后）| 120 | — | **+0.049%** |
  | breakout-long | 122 | +0.075% | +0.002% |
  | kdzx-long | 211 | +0.034% | **−0.043%** |

  - `kdzx-short`：③双顶/死叉 + ④箱体 两段换成一段「放量下破前 20 根区间」，
    超额 +0.004% → +0.049%。等双顶走完再等 12 根箱体，反抽那波已经结束了。
  - `kdzx-long`：八个变体的消融全是负超额（去掉/放宽/缩短箱体、去掉/放宽贴近
    均线、只要金叉、只要双底、换突破扳机），不是参数问题。超额为负意味着
    "它挑出来的时刻比随机时刻更差"，与市场涨跌无关。**按用户决定仍保持启用** ——
    它只是发提醒，不替人确认；但样本只有两个月单一上涨行情，结论本身也不牢。
    复核的正确做法是换一段震荡或下跌行情重跑，**不是调参**。
    数据与消融结果完整留在文件头。

### Fixed
- **网格的两处回归**（预警组上线时引入）：`zoomCell` 与 `syncCrosshair` 都只遍历
  周期模式的格子、且按 `tf` 而非格子身份匹配 —— 预警组里**双击放大整个失效**
  （连带"放大后才允许缩放"也失效）、**十字线同步不生效**。改走 `activeCells()` 并按 `key` 比对。
- **`pin_tls.py` 不读 `.env`**：用 `setup_env.py` 配好用户级凭据后再跑它，仍然报
  「请先 `set -a; . ./.env; set +a`」——那是 bash 语法，Windows 上照做也没用。
  现在自己 `load_env()`，并支持 `--write` 直接写回（不用手抄 64 位十六进制）。
- **冷启动默认选中一个没数据的标的** → 一打开就是空图，看着像面板坏了。
  改成优先选有数据的；一个都没有时给出"先回补 / 先跑 watch"的说明，而不是留张空图。
- **信号流的「全部标的」筛选照抄注册表**，与旁边按信号条数生成的「全部规则」不同源 ——
  选一个从没触发过的标的必然是空列表。改成同源同做法。

### Added
- **`backfill.py --timeframe 1d`**：直接拉日线，只聚合周线月线。
  高周期原本全靠 1m 聚合，于是"能看多少根日线"取决于回补了多长的 1m
  （三个月 1m = 45 根日线 / 10 根周线 / 3 根月线）。接口原生支持日线，
  实测 rb2610 日线 45 → 261 根。`CalendarBuilder` 相应放宽为"接受任何更小的周期"。
- **`watch.py` 单写者保护**：同一运行态已有进程在跑时拒绝启动并打印对方 pid 与启动时间。
  跑两个的话状态机、去重表各持一份内存态，同一根 bar 判两次、**重复报警且不报错**。
  用 SQLite 里的 pid 记录 + 存活探测，不用 pid 文件（那玩意崩溃后会留下，反而把自己挡在门外）。
- **标的下拉框分组 + 标注**：「有信号」/「其他已注册」两组；每项标出点进去可能是空的原因
  （`无数据` / `数据止于 X` / `未盯` / `主连`）。`/api/meta` 相应补 `last_day`
  （只列 Parquet 分区目录名，不读文件）。
- 文档：README 加「我该跑哪个」决策表与「工具箱」分节（十几个脚本里只有两个会长期运行）；
  `docs/RUN-WINDOWS.md` 加备份一节（**`runtime.sqlite3` 必须连 `-wal` 一起拷**）。

### Added（预警组）
- **预警组：九个标的同屏**（网格的第二种模式，设计画布见 `docs/design/watchlist/`，
  口径见 ADR-0013）。九格一格一个标的、整组一个周期（默认 5m），按市场分 tab
  （期货 / 加密，未读数只在有未读时出现）。
  - **组是算出来的，不是维护出来的**：`钉住的 ∪ 最近触发的`，取前九，钉住的在前。
    只有「钉住」落库（`watchlist_pin`，schema v3），其余每次重算 ——
    没有淘汰逻辑，也就没有"手动删了又被自动加回来"这类状态同步 bug。
  - **钉住的永不被挤掉**，那是人工判断「还需要观察」的唯一表达；
    钉住数超过九个时多出来的照样返回并如实提示，不悄悄砍掉一半。
  - **钉住即采集**：`watch.py` 启动时把钉住的并入规则 universe。
    没规则盯的格子如实说明"盯盘进程不采集它的行情"，不留一个静默的空。
  - 与既有「一标的×九周期」**共用同一套格子构造**：两处各建一套的话，
    同一条信号在两种模式下会落在不同高度 —— 最容易被读错又不会报错的那种不一致。
  - 钉住按**绑定地址**放行（默认 127.0.0.1 可用，`--host 0.0.0.0` 拒绝并给出 SSH 隧道做法），
    不占用 `--allow-edit` —— 那个开关是给规则 CRUD 和试算准备的，量级不同。
- **买卖点标记重做：B/S 徽章 + 成对交易带**（方案 B+C，设计画布见
  `docs/design/markers/`）。改用 **series primitive 自绘层**，不再用内置
  `setMarkers` —— 内置 marker 的位置只能贴 bar 上/下方，**徽章因此不在价格上**，
  设计稿里的胶囊底片、半透明交易带、盈亏 chip 也全都无从表达。
  自绘层用 `priceToCoordinate`，几何真正锚在触发价/成交价上；九宫格与单图共用同一层。
  四处变化：
  - **密集处折叠成「B×3」**：同一根 bar、同一方向的多条信号折成一枚，
    详情面板列出全部成员、可点开。**多空不折叠** —— 同一根上多空同时触发是
    矛盾信息，红绿各留一枚才看得见，折成一个代表等于把矛盾藏起来。
  - **成对交易带**：开仓与它的离场用虚线连成一笔，颜色随盈亏（赚绿亏红）。
    持仓中的**不画连线** —— 它还没有终点，画到最后一根会被读成"在那里平掉了"。
    实现上是**一笔一条 series、每条只有两个点**：lightweight-charts v4.2 的
    LineSeries **不按空白点断线**，"一条装全部 + 空白点断开"的两版都被画成了
    一条贯穿全图的假线（截图才发现，数据一直是对的）。
  - **成交点常态带文字，两端分工**：开仓写价格（你付了多少），离场写盈亏
    （这笔结果如何）。两端都写"价格+盈亏"试过了，密集处两个离场标签直接压在
    一起还被烛身盖掉；标记文字没有底片、盖不住 K 线，唯一的解法是把字变短。
- **`priority` 收成枚举**（`high` / `normal` / `low`），loader 拒绝未知值。
  折叠时的排序键 `rank_key` 是**全序**：tentative → 声明档位 → 扳机周期（大的优先）
  → 链路长度 → rule_id 兜底。少了最后一级，各项并列时代表的选择取决于字典迭代
  顺序 —— 回放与实盘会折出不同的代表，直接违反"逐条一致"这条红线。
- `web/markers.py`：折叠与配对**算在服务端**（与分桶同理，前端只负责画），
  因此可以脱离 web app 单测。
- 图表脚注改成「标注 N 枚 / 信号 M 条（K 处叠了多条，已折成 ×N）」。
  折叠后枚数 < 条数是正常的，光写「标注 3/4」会被读成"1 条丢了"。

- **「看大做小」全品种规则**（`config/rules/kdzx-long.yaml` / `kdzx-short.yaml`）+
  `TESTING-KDZX.md` 真机测试说明。7 个品种（rb/au/cu/IF/m + BTC/ETH）。
  **阈值一律 ATR 归一**（`abs(close-ema) < atr(14)*0.8`）而非百分比 ——
  黄金日内波动 ~0.3%、BTC ~3%，同一个百分比对两者是两回事。
  逐品种试算验证：7 个价格量级从 960 到 78000 的品种全部能触发（4~51 条）。
- **量能均线**（单图 VSMA5/20，九宫格 VSMA20），画在成交量那条价格轴上。
  **窗口 20 不是随便挑的**：内置规则 `volume-spike` 判的就是
  `volume > sma(volume, 20) * 2.5` —— 图上这条线和规则看的必须是同一个数，
  否则看不出它当时为什么触发。同样复用引擎的 SMA。
- **均线与成交量**（单图 SMA5/10/20/60，九宫格每格 SMA5/20；量柱在同一窗格底部）。
  - **均线算在服务端**（`web/overlay.py`），复用引擎的 `SMA`/`EMA` 类。
    前端自己拿 close 重算，口径一偏（ADR-0006：EMA 用 SMA 播种、预热返回 None）
    就会出现「图上明明上穿了、规则却没触发」—— 那类问题查起来极其费劲。
  - 均线在**完整序列**上算再截取：只用截出来的 220 根算 MA60，图上左边会缺一截。
  - 预热期是 `null`，前端**跳过**这些点 —— 补 0 会在图左端画出一条从零飙起来的假线。
  - 量柱跟着当根阴阳着色，和 K 线一个颜色语言。
- **信号流筛选框改为「信号里的规则 ∪ 已加载的规则」**，每条带信号条数，
  已删除的标「已下线」。实测库里 50 条信号有 44 条来自早已删掉的 `multi-level-verify` ——
  只列已加载的规则，那 44 条永远筛不出来；而列表里的规则可能一条信号都没有。
  空态也按"选了哪条规则"分开说明。
- **九宫格多周期十字线同步**：在任意一格悬停，其余八格显示**同一时刻**的十字线；
  `shift + 单击` 锁定该时刻（鼠标移开也不跑），`Esc` 或再次 shift+单击解除。
  - **跨周期对齐到"包含该时刻的那根 bar"**：1m 上的 10:03 在 1d 上不是一根 bar，
    直接把原时刻塞给日线图，十字线要么不显示、要么落在错的位置。
    实测锁定 30m 的 22 May 10:00 → 1d 落 21 May 15:00、1w 落 15 May 15:00、
    1mon 落 30 Apr 15:00。
  - **防回环**：程序设置十字线会再次触发 `crosshairMove` 回调，不加锁就是无限递归。
- **`Esc` 退出九宫格放大**（先解除十字线锁定，再退出放大）——
  放大后没有可见的关闭按钮，只能双击，全屏状态下很不直觉。
- **脚本自己加载 `.env`**（`core/env.py`）+ **`scripts/setup_env.py` 一次性配置**。
  原先文档教的是 `set -a; . ./.env; set +a` —— **bash 专有写法，Windows 上没有对应物**，
  于是每开一个终端都要手动 `set` 一遍，这才是"每次都要重配"的根因。
  - 查找顺序 `SIGDESK_ENV` → `./.env` → **`~/.signal-desk/.env`**。
    最后那层是关键：分发包不含 `.env`，换新包时项目内那份就没了。
  - **已在环境里的键永远不被覆盖**，显式 export / CI 注入优先。
  - 诊断只打印键名不打印值；密钥用 `getpass` 读，不回显、不进 shell 历史。
  - **不做前端配置界面**：面板无鉴权，"改规则文件"和"写 API 密钥"是两个风险等级。
- **分时图**（九宫格第一格，`/api/intraday`）：当日价格线 + **累计均价线**。
  分时不是一个"周期"，是当日 1m 的一种画法，所以**不进 `Timeframe` 枚举** ——
  进了会污染聚合/规则/回测，而它们跟"怎么画"无关。
  - **均价 = 累计成交额 / (累计成交量 × 合约乘数)**。乘数不能漏：rb 每手 10 吨，
    `money/volume` 得到的是 31320 而不是 3132 —— 漏了均价线会画在十倍高的地方。
    乘数和"当日"（`trading_day`，夜盘归属下一交易日）都只有服务端有，故整段算在服务端。
  - 取**数据里最后一个交易日**而不是墙钟今天：数据可能是陈的（进程没跑、周末），
    画出上一个交易日并把日期标出来，比画一张空图诚实。
  - 成交额缺失时均价是 `None` 而非 0，前端也不画那些点（ADR-0006）。
- 九宫格排布：`分时/1m/5m`、`15m/30m/1h`、`1d/1w/1mon`（用独立常量 `GRID_TFS`，
  不复用 `meta.timeframes` —— "系统支持哪些周期"和"我要同屏看哪九个"是两件事）。
- **九宫格：同一标的的 9 个周期同屏**（盯盘页「九宫格」按钮），点任意一格**放大铺满**、
  再点还原。看大做小要的是同时看见大级别形态和小级别时机 ——
  来回切周期会丢掉"大级别此刻长什么样"。
  - 新增 **`1w` / `1mon` 周期**：与 `1d` 一样按自然日历切分（`CalendarBuilder`）。
    周键用 **ISO 年-周**（`isocalendar()` 的年）—— 12-29 属于次年第 1 周，
    用日历年会得到 `2025-W01` 排在 `2025-W52` 前面，凭空炸出"时间倒流"。
    月键 `YYYY-MM` 补零，否则 `2026-9` 会排在 `2026-10` 后面。
  - `Timeframe.rank` 覆盖全部 9 个周期；`is_calendar` 区分日历型与墙钟型。
  - **`DEFAULT_TIMEFRAMES` 补齐日/周/月** —— 少一个不会报错，只会让那一格静默停更。
  - 每格 220 根（小格子塞 1500 根就是一团黑）、信号标注每格都画
    （同一条信号在 1m 和 1d 上位置不同，那正是要看的）、小格禁用拖拽缩放（误拖会乱）。
- **信号提醒：弹窗 / 声音 / 语音播报 / 桌面通知**（面板右上角四个开关，选择存 localStorage）。
  盯盘时信号到达会立刻提醒，不必一直盯着屏幕。四条约束都有测试钉住：
  - **只有 SSE 推来的新信号才提醒** —— 开机灌进的几十条历史信号一条都不响；
  - **一批同时到达只响一次** —— 多标的同刻收盘很常见，一根 bar 响五声等于噪音；
  - **AudioContext 在用户手势里创建/恢复** —— 点"声音"开关本身就是那个手势
    （浏览器自动播放策略），开的时候立刻响一声示范；
  - **每个浏览器 API 都可能缺席或被拒绝**（无痕、非 https、拒绝授权），全部 try/catch，
    任何一个缺失都不能让面板起不来。桌面通知被拒时开关标红，不装作已开启。
  提示音用 Web Audio 现场合成（多头上行两声／空头下行两声／中性单声），
  不引外部音频文件（ADR-0009 无构建单页）。语音播报读"标的 方向 价格"。
- **离线验收脚本 `scripts/acceptance.py` + `run_acceptance.bat` + `TESTING.md`**：
  一条命令跑完 49 项检查（质量/数据日历/规则引擎/面板 API/规则读写/视觉），
  全程离线、不需凭据、不下单。给 agent 用的 —— 不必理解散文清单，看 exit code 即可。
  末尾明确列出**自动化验不了、只能人工确认**的三件事。
- `shoot_panel.mjs` 跨平台找浏览器（Windows 的 Chrome/Edge、macOS、Linux），
  也可用 `SIGDESK_CHROME` 指定。
- **跨级别引用 `at('1h', <表达式>)`**（ADR-0011）：把整个子表达式放到另一个周期上求值。
  做成**特殊形式**而不是给三十多个指标各加一个周期参数 —— 一处改动解锁全部现有与
  将来的指标/形态原语。as-of 由外层视图定死，5m 那刻只看得到已收盘的 1h（INV-1/INV-2）。
  - `Rule.required_timeframes` / `store_timeframes(rules)`：规则自报需要派生哪些周期。
    **少派生一个不会报错，只会让该级别恒"不成立"**，所以周期必须字面量、取不到就当场抛。
  - 实测意义：同一策略"1h 收盘时判贴近"与"5m 那刻判贴近"是 41 条 vs 37 条信号，
    之前那个近似不是无害的。
- **日线（`1d`）可用作条件级别与 `at()` 目标**（ADR-0011）：`DayBuilder` 按**交易日**
  聚合，**夜盘归属下一交易日**（08-27 21:00 那根属于 08-28）。按自然日切会把每个
  交易日劈成两半且不报任何错。当日未走完的日线永不吐出（INV-2）。
  新增 `Timeframe.rank`（`D1.seconds` 是 0，按 seconds 排会把日线排到 1m 前面）。
- **`prev(x)`**：暴露 `Level`/`Series` 本来就有的"上一根"。
  注意语义随来源而变 —— `prev(close)` 是上一根 bar，`prev(swing_low(5))` 是**上一个摆动低点**。
- **`double_bottom(n, tol)` / `double_top(n, tol)`**：除了两点价格接近，还检查**颈线** ——
  没有像样反弹的两个相近低点不是双底，是横盘。数据不足返回 None 而非 False（ADR-0006）。
- 回补脚本（`backfill.py` / `build_continuous.py`）同时产出日线分区。
- **规则增删改查 + 语法校验 + 历史试算**（PRD FR-5.3，面板新增「规则」页）：
  - `rules/store.py`：只写**校验通过**的（`config/rules/` 会被真加载且 fail-fast，
    写坏一个就是下次启动失败）；原子写；**删除移进 `_trash/` 不真删**；
    id 走白名单正则（id 直接拼进路径，不限制就是路径穿越）。
  - `rules/trial.py`：历史试算。**复用同一个引擎**（ADR-0001），所以试算与实盘/回放
    逐条一致；有对拍测试钉住。试算不落盘、不推送、不下单、不读写运行态。
  - **写端点默认关闭**：面板没有鉴权。`serve.py --allow-edit` 才开，且**只允许绑回环地址**；
    盯盘进程（`watch.py --web`）里一律拒绝 —— 热替换会静默丢掉已布防的链路。
    试算同样过这道门（它会按请求读任意标的的全量历史并同步跑引擎）。
- **「各级别条件成立区间」带状图**（设计稿里有、之前没实现）：
  走**按需重算**而非新增持久化 —— 不占存储、能覆盖规则创建之前的任意历史。
  - `RuleEngine(recorder=...)`：条件判定的**只读旁路**。做成钩子而不是让调用方重算一遍，
    是因为 ADR-0007 拒绝第二条求值路径；画的必须和引擎看到的是同一份。
  - 同时记 `raw`（表达式自己的真值）与 `satisfied`（套上 mode 后链路看到的）。
    「表达式明明成立、链路却不认」是"为什么没触发"最常见的答案。
  - 没有任何区间时不撂死话，改为摊开逐级判定次数：全 unknown = 指标没预热完，
    全 false = 条件太严，两者的下一步完全不同。
- **期货主连拼接**（`store/continuous.py` + `scripts/build_continuous.py`）：
  用 `main-by-date` 的换月区间 + **真实合约**数据合成连续序列，解开"跨换月无法回测"
  这个期货侧的硬约束（数据源的 8888 口径不可复现，坑#9）。
  - 默认**后复权价差**：最新一段保留真实价格，历史段整体平移，指标不会在换月处
    吃到假跳空。实测 2026-04-08 rb2605→rb2610：复权后接缝 −2，不复权则是 −26。
  - 换月锚点取**两个合约都有成交的同一根 bar**；拿"旧合约末根"对"新合约首根"
    会把隔夜跳空算进价差。
  - 平移量逐次写进产物元数据 `data/bars/_continuous/<uid>.json`。
  - 缺合约、无重叠锚点、平移后价格击穿零点，一律 fail-fast，不静默跳过。
  - `Symbol.main_code`（如 rb9999）：只用于查换月区间；主连标的**故意不给 `quote_code`**，
    没有 quote_code 就没人能直接去拉它的 K 线。
  - 夹具 `tests/fixtures/main_by_date_rb_i.json`（rb/i 两品类 11 段真实换月）。
- **2026 中国法定节假日表**（`config/calendars/cn_futures.yaml`）：19 个落在工作日的休市日，
  由两个独立来源交叉核对（33 个休息日，零分歧），条目附来源注释。
- `MarketCalendar.opens_night()` / `is_trading_date()` / `Session.is_night`：
  显式建模「节假日前最后一个交易日不开夜盘」（夜盘归属下一交易日，那天不开市）；
  周末不适用 —— 周五夜盘照开，归属下周一。
- `MarketCalendar.trades_on_weekends`（加密置 True，**默认 False**：忘配的后果是
  周末误判成休市，比反过来安全）。显式节假日条目仍然生效，不受该开关豁免。
- 日历回归测试 13 项，含一条专钉 YAML 类型归一的「静默失效防线」，
  以及一条直接加载真配置、断言国庆当天确实休市的端到端断言。单测 487 → 500 项。
- 项目设计文档：PRD、ARCHITECTURE、ROADMAP、CONVENTIONS、DEVLOG
- ADR-0001 单引擎三模式；ADR-0002 行情与交易通道分离；ADR-0003 形态层三档；ADR-0004 存储选型
- `core`：Bar/Symbol/Timeframe 模型、墙钟分桶、交易日历、标的注册表
- `feed/quote_api`：Quote API 异步客户端（TLS 指纹固定、指数退避重试）与纯函数归一化
- `store/bar_builder`：1m→高周期增量聚合，增量与批量结果一致
- `store/parquet_io`：按 market/symbol/timeframe/交易日 分区落盘，重写幂等
- `scripts/`：`pin_tls.py` 证书指纹引导、`backfill.py` 历史回补、`crosscheck.py` 对拍验收工具
- 51 项单测；对拍夹具 `tests/fixtures/rb2610_bycount.json`（归档段 + 当日段）
- **M0-B 加密数据底座**：
  - `feed/okx`：OKX 永续 REST 客户端（限流 + 指数退避 + 分页回补）与纯函数归一化。
    显式建模与期货相反的语义：`ts` 为开盘时刻、返回降序、收盘取 `confirm` 字段不靠本地时钟。
  - `feed/okx_ws`：WS 实时 Feed —— `candle1m` 订阅、裸字符串心跳、指数退避重连、
    重连后按缺口区间用 REST 自动回补，去重复用期货侧的 `BarCursor`。
    连接抽象为 `WsConnection` 协议，重连与回补逻辑可完全脱网单测。
  - `store/bar_store`：`BarStore` + `BarView`，INV-1 的落地。喂 1m 增量派生高周期，
    `view(symbol, as_of)` **在构造时**定死截断位置，两个市场共用同一实例。
  - `feed/polling`：`BarCursor.seed()` —— 用已落盘位置播种游标，使**进程重启**造成的
    停机缺口与断线缺口走同一条回补路径（`OkxWsFeed(resume_from=...)`）。
  - `scripts/crypto_live_check.py`：加密实时链路验收工具（WS 跑 N 分钟后与 REST 逐字段对拍；
    `--drop-after N` 强制断线并压住 75s，验证真实重连 + REST 回补）。
  - ADR-0005 加密交易所选型；`config/symbols.yaml` 注册 OKX BTC/ETH 永续。
  - 夹具 `btcusdt_swap_okx.json`（1m/5m/15m/1H 同窗口归档段）与
    `btcusdt_swap_okx_ws.json`（同一根 bar 的 WS/REST 配对，留证字符串格式差异）。
  - 单测由 51 项增至 117 项。
- **M1 指标 + 形态 + 单级别预警**：
  - `indicators/`：增量指标 SMA/EMA/Wilder/StdDev/RSI/MACD/BOLL/ATR/KDJ，
    O(1) 更新、预热期返回 None。口径（EMA 播种、MACD 柱 ×2、BOLL 除数、Wilder≠EMA）
    定死在 ADR-0006 并用**独立的朴素全量重算**参照实现逐点对拍。
  - `patterns/`：白名单 AST 表达式引擎（A 档）、结构原语（B 档：range/breakout/
    swing_high/swing_low/gap/engulfing/pin_bar/inside_bar/consolidation/highest/lowest）、
    `@pattern` 插件注册（C 档）与受限 `PatternCtx`。三档同一张函数表，可自由混用。
  - **三值逻辑**：指标预热期为 None 并向上传播，`is_satisfied` 把 None 当作不成立 ——
    预热期宁可不报也不误报。
  - `rules/`：单级别规则模型、YAML 加载（加载期编译校验表达式）、
    `RuleEngine`（state/event 两种模式、按 bar 时间的冷却与去重、关键值快照、`prime` 预热）。
  - `sinks/notify.py`：Telegram / Bark / 控制台推送，单渠道失败不影响其他渠道也不打断行情处理。
  - `scripts/watch.py`：盯盘入口（WS/轮询 → BarStore → 规则 → 推送，启动先预热不发历史信号，
    单根 bar 出错只跳过并告警、不让盯盘整个退出）。
  - `store/bar_store`：`last_close_ts` / `resume_map`，`feed/polling`：`PollingFeed(resume_from=...)`
    —— 预热后给 Feed 游标播种。不播种时首轮轮询会把重叠窗口当新数据重发（真跑重发 17 根）。
  - 规则示例 `config/rules/ema-cross-long.yaml`、`volume-spike.yaml`；ADR-0006 指标口径。
  - 单测由 117 项增至 298 项。
- **M2 多级别规则引擎【核心里程碑】**：
  - 规则模型泛化为**有序条件链路**（顺序即链路顺序，最后一条是扳机）。段数任意，
    长度 1 即单级别 —— M1 与 M2 **共用一个模型、一个引擎**（ADR-0007）。
  - `rules/state.py`：状态机（推进 / 回退 / TTL / 冷却），纯逻辑、可完整序列化。
  - `rules/engine.py`：求值分**记账**与**判定**两步，入口 `on_bars(批)`；
    `snapshot`/`restore`/`cursor`/`resume` 支持进程重启。
  - `store/runtime_store.py`：SQLite(WAL) 持久化状态机、条件日志、去重表与信号表。
  - `feed/replay.py`：`ReplayFeed` 历史回放，与实盘共用同一条引擎路径。
  - `scripts/replay_check.py`：红线验收工具（live 落 Parquet → 回放 → 逐条对拍）。
  - `rules/loader.py`：支持 `timeframes` 角色映射、`window`/`within`、`ttl: N bars`、
    `context_by_role` 各级别快照；加载期编译校验一切。
  - ADR-0007 多级别状态机语义。单测由 298 项增至 363 项。
- **M3 Web 只读面板 + 信号质量统计**：
  - `stats/`：信号结果标注（固定持有期 + 止损止盈两种口径同时生效）与质量报告
    （触发次数、胜率、期望收益、盈亏比、假信号率、MFE/MAE、分规则/品种/方向/时段）。
    口径见 ADR-0008：入场价默认取**次根开盘**、同根同时触及止损止盈**记止损**、
    成本按**双边**扣、`neutral` 不进胜率。
  - `web/health.py`：运行健康（Feed 连接/重连/缺口/回补、各标的新鲜度）；**休市不算滞后**。
  - `web/api.py`：只读 API（meta / health / signals / bars / markers / stats）+ SSE 实时流。
    SSE 每个订阅者一个**有界**队列，满了丢自己的旧消息，绝不反压到行情处理。
  - `web/static/`：无构建单页面板（ADR-0009），内置 lightweight-charts，不依赖 CDN。
  - `scripts/serve.py`（独立只读）、`scripts/report.py`（终端版质量报告）；
    `scripts/watch.py --web` 与引擎同进程起面板并落盘预热历史。
  - `tests/test_panel_js.py`：前端 JS 的 node 桩化冒烟（无 node 时跳过）。
  - ADR-0008 统计口径、ADR-0009 前端选型。单测由 363 项增至 418 项。

- **M4 策略层 + 纸上撮合**（ADR-0010）：
  - `trade/model`：Intent / Fill / Position / Account，幂等锚是 `Signal.dedup_key`。
  - `trade/strategy`：三种定量方式（风险/固定/名义），止损口径与 ADR-0008 **共用同一函数**；
    定量时按三个硬上限的最紧者截断。
  - `trade/risk`：FR-7.2 的五条风控 + 幂等去重，全部按 **bar 时间**判定，一处不读墙钟。
  - `trade/paper`：挂单→次根开盘成交、滑点按方向恶化、费用双边、
    同根同时触及记止损、开仓那根也判出场。
  - `trade/desk`：把四段串成链路，每根 bar 先撮合再产信号。
  - `config/trading.yaml`（**默认关闭**）、`scripts/paper_run.py`（纸上回测）、
    面板「纸上账户」页、`/api/trade` 端点、成交与账户落 SQLite（schema v2）。
  - 单测由 432 项增至 487 项。

### Fixed
- **跨零点夜盘的凌晨段，跨周末时交易日归错**（`MarketCalendar.trading_day`）：
  周五夜盘 23:59 归下周一、同一场的周六 00:00 却归周六 —— **一场夜盘被劈成两个交易日**。
  后果是日线聚合抛「时间倒流」、Parquet 分到两个分区；接上 au2610（夜盘到 02:30）后
  `watch.py` 直接崩在启动阶段。`in_session` 一直判对了，只有 `trading_day` 没跟上 ——
  两个本该同源的判定各写各的。**修完历史数据必须重补。**
- **`backfill.py` 跨度大就读超时**（实测两个月必挂、一周稳过），且超时抛在流读取阶段、
  看起来像"连不上"。改为内部按周分段拉（幂等）。
- **期货历史按标的降级**：5 个品种里 1 个拉失败，原先整个期货市场被跳过。
  现在只丢拉不动的那个。
- **任一行情源连不上就整体启动失败**：加密挂了连能用的期货也盯不了。
  改为**按市场降级** —— 一边失败只警告并跳过那一边，两边都挂才算真的起不来
  （并说明原因与排查方向）。缺凭据单独提示缺哪个键、怎么配，不再抛 `KeyError`。
  用 hosts 污染 OKX 与抽掉凭据两种方式实跑验证过。
- `.env.example` 里残留真实行情主机名，已换成占位符（测试钉住不再回归）。
- **下拉框里有、却从没被采集过的标的**：面板列全部注册标的（为了能看主连），
  但 `watch.py` 只采集**出现在某条规则 `universe` 里**的标的。
  于是 `IF2609/m2701/cu2610` 在下拉框里存在、选中却是一张空图 ——
  用户只会以为"期货连不上"（真发生过）。现在：
  - `/api/meta` 每个标的带 `watched`，下拉框标出「**· 未盯**」；
  - 空状态按"有没有人盯"分开说明，并给出下一步（加进规则 universe，或 backfill 回补）。
- 图表空状态里的 `<b>`/`<br>` 被 flex 容器拆成多个 flex item 排成一行，换行全乱。
  内容裹进一层块级元素即可（截图当场看到）。
- **九宫格单击就放大**，导致没法停在小格里看十字线对比多周期。改为**双击**放大/还原。
- **日/周/月格子里的信号箭头被撑到半个格子**：lightweight-charts 的标注尺寸随 bar
  宽度缩放，只有一两根 bar 时每根极宽。少于 20 根就不画标注 ——
  盖住 K 线不说，还让人误以为那是个特别重要的信号。
- **`#grid-toggle` 按钮没插进 index.html**（字符串替换缩进不匹配，静默失败），
  真实浏览器一开就白屏 —— 而**桩化冒烟照样绿**：DOM 桩对任意选择器都返回节点，
  HTML 里少一个元素它完全看不出来。现在冒烟会把 app.js 查过的 `#id`
  与 index.html 对一遍（`missing_ids`），并验证过"删掉按钮它确实会红"。
- 九宫格格子标题曾用 `.tf` 类名，与顶部周期按钮撞名 —— 选择器互相误伤，
  截图脚本点 `.tf` 直接超时。改为 `.cell-tf`。
- 小格子里 TradingView 角标正好压在 K 线上（9 格就是 9 个），
  用 v4.2 的 `layout.attributionLogo: false` 关掉（单图页保留，那里不碍事）。
- 「点击放大/还原」提示与格子标题都绝对定位在右上角，叠成乱码。提示挪到左下。
- **`neutral` 方向的信号被画成向上箭头**（灰色）：会被读成"看多"。改为灰色圆点。
  多=绿上箭在 bar 下方／空=红下箭在 bar 上方，三者一眼分得开。
- **成交价完全没画到图上**：`/api/markers` 只返回信号，纸上撮合的开仓/止损/止盈价
  一条都看不到。现在信号与成交**分开画**，成交点带价格文字；选中信号时把触发价与
  对应成交价画成横线 —— 箭头只说明"哪一根"，横线才说明"哪个价"。
  成交沿用与信号**同一套服务端分桶**，避免"箭头在这根、成交在那根"。
- **新建的规则在盯盘页的信号流筛选框里找不到**：`#f-rule` 只在 `boot()` 填一次，
  保存规则后 `S.meta` 没更新，要刷新整个页面才看得到。抽出 `fillRuleFilter()`，
  保存/删除后重拉 meta 重填，且重填不冲掉用户当前选中的筛选项。
- **规则编辑器的文字是纯黑，深色底上看不见**（对比度 1.17:1）：基础表单样式
  `select,input,button{color:var(--fg)}` **漏了 `textarea`**，而表单控件不继承
  页面的 `color`。唯一显式设过颜色的是 `#rule-src:disabled` ——
  **截图恰好只截到那个禁用态，所以一直没被发现**。
  同时给 `::placeholder` 显式上色（各浏览器默认不一，Firefox 还带 opacity）。
- `shoot_panel.mjs` 新增**对比度检查**（阈值 3:1，只抓"基本看不见"，
  不管有意做淡的次要文字），并补截「新建规则」那一屏 ——
  编辑器**启用态**的文字颜色只有在那个状态才看得到。
  撤掉修复后该检查确实报错并以非 0 退出，验证过。
- **`watch.py` 只派生规则用到的周期，导致图表周期静默停更**（今天引入的回归）：
  它同时喂两个东西 —— 规则引擎（只要规则用到的）和面板图表（要全部可选周期）。
  规则只用 15m 时，5m/30m/1h/4h 全部不再更新，**面板上看着就是「行情连不上了」**。
  改为 `DEFAULT_TIMEFRAMES ∪ store_timeframes(rules)`，与 `paper_run`/`replay_check` 一致
  （那两个当时就用了并集，唯独漏了最该用的这个）。
- **`watch.py` 不加 `--web` 时行情完全不落盘**：盯盘进程跑得好好的，面板却一直是
  几天前的数据。改为**一律落盘** —— 盯盘却丢掉自己收到的行情，本身就说不通。
- 图表页脚补上「末根多久以前」。只陈述事实不判断对错（判断"该不该有新数据"要看日历），
  但「你正在看三天前的数据」必须一眼可见 —— 进程没在跑时图看着完全正常、只是永远不动。
- **`/api/rules/{id}` DELETE 返回平台相关的路径分隔符**：Windows 上是
  `rules\\_trash\\x.yaml`，任何按路径断言或展示的调用方跨平台就炸
  （Windows 验收时当场挂了一条测试）。改为一律 `as_posix()` —— **JSON API 的
  路径表示是接口约定，不该跟着操作系统变**。
- **验收脚本把「环境缺东西」记成失败**：那台 Windows 机器没装 playwright，
  截图组报了 2 条假红。现在区分 **❌ 失败 / ⏭️ 跳过**，退出码只看失败项，
  跳过项单独列出并写清怎么补齐。否则每台新机器都冒假红，真缺陷会淹没在里面。
- **`run_acceptance.bat` 的两个可用性问题**（Windows 验收暴露）：
  `pause` 在 agent/CI 的非交互 shell 里永远挂住（改为默认不 pause，
  双击场景用 `--pause`）；`python` 常指向 3.11 不满足 `>=3.12`
  （改为依次试 `py -3.14/-3.13/-3.12/-3`、`python3`、`python`，找不到明确报错）。
  日志落盘挪进 `acceptance.py --log`：**cmd 的管道会让 `%ERRORLEVEL%` 变成管道
  最后一个命令的返回值**，用管道 tee 会把退出码丢掉。
- **`types-PyYAML` 从未写进 `pyproject.toml`**：我的开发环境手装过，所以本地
  mypy 一直是绿的；**干净环境里会报 4 个 import-untyped 错**。打测试包、解压到
  空目录重跑才发现 —— 「本地能过」不等于「别人能过」。
- **回补脚本与面板的落盘路径不一致**：`backfill.py`/`build_continuous.py` 写 `data/bars/`，
  而 `watch.py`/`serve.py`/`report.py`/`paper_run.py` 读写 `data/` —— **回补的数据面板
  一根都读不到**。六个脚本统一到 `data/bars/`，已有的 `data/CRYPTO/` 一并迁入。
- **图表空状态被 lightweight-charts 的 canvas 完全盖住**：元素在、文字在、visibility
  正常，屏幕上却一片漆黑，看着像加载失败（canvas 是 z-index:1/2 的绝对定位层）。
  截图看得见症状但说不出原因，靠 `elementFromPoint` 命中测试才定位到。
  `shoot_panel.mjs` 现在每张截图后都跑一遍**遮挡检查**，把这条变成常驻不变量。
- **换标的/换周期后可视区间不重置**：从 259 根的 5m 切到 61 根的日线，
  旧区间会让新数据缩在右边、左边空掉三分之一。没有可定位信号时显式 `fitContent()`。
- **主连选不到**：`/api/meta` 用的是 `registry.tradable()`，它排除主连 ——
  于是自己拼出来的连续序列在面板下拉框里根本不存在。改为列全部标的并标出「主连」；
  排除主连是「预警/下单」路径的事（`tradable()` 仍排除），不是看图的事。
- **函数参数个数不在编译期检查**：`ema(close, 60, '1h')` 能编译通过，要等**上线后
  第一根 bar** 才抛 TypeError —— 而 `config/rules/` 是 fail-fast 加载的，坏规则本不该
  进得了生产。现在 `FuncSpec` 带 `inspect.signature`，编译期就核对形状。
  同时拒绝 `f(**d)`（其 `keyword.arg` 是 None，求值器会静默丢掉，参数凭空消失）。
- **面板把"算不出来"显示成 0**：0 条可评价信号时，质量统计页与规则试算都会显示
  「胜率 0.0%」「期望收益 +0.000%」—— 那不是不赚钱，是没有样本。现统一显示破折号。
  与 ADR-0006「预热期是 None 不是 0」同一条原则（M4 已在收益率上栽过一次）。
- **`[hidden]` 被 `.badge{display:flex}` 盖掉**（类选择器优先级高于 UA 样式表）：
  「未保存」徽标一打开规则就亮着。补 `[hidden]{display:none!important}`。**截图才看得出来。**
- **`confirm_on_close: false` 静默无效**：规则里能写、加载器也认、`Signal.tentative`
  字段与落盘/推送/面板分支全都在，**唯独没有任何一处会把它置为 True** —— 用户以为
  开了盘中预报，实际还是只在收盘报。盘中预报实现之前，加载器直接拒绝启动。
  同时要求它必须是真布尔值（`bool("false")` 是 `True`，用 `bool()` 兜是第二个陷阱）。
- **面板前端按设计稿重做**（ADR-0009 的设计稿见 `docs/design/`）：
  - 新增顶部**规则链路状态条** —— 哪些标的已布防、TTL 剩几根、谁在冷却。
    配套新增只读端点 `/api/chains`（只导出引擎已有的内存状态，无副作用）。
  - 选中的信号展开成**「为什么触发」**：逐级别给出条件表达式、该级别的快照值与所属 bar 时刻，
    替掉原来一行截断的 `k=v`。规则已下线时按 context 的角色前缀降级渲染，不再把值整个吞掉。
  - 统计页：**结论排在第一张卡**（原来埋在 10 张一样的卡片里）；退出原因改为直接标注的分段条；
    新增「价格走到了哪里」把平均浮盈浮亏与止损止盈距离画在同一根轴上；
    分时段由 17 行表格改为柱状图，**样本不足时明说**（阈值 10 条 —— 5 条算出的胜率和抛硬币没区别）。
  - 健康页：整体状态一行说完；标的那块改为**连续性时间线**（缺口计数只说"有 1 个"，
    时间线告诉你"什么时候、多久、是不是在交易时段内"）。
  - 图表标题栏加 OHLC 读数并随十字线联动；空数据、空信号、空统计都有明确的空状态。
- 面板：K 线标注不再写规则名（21 个同名标注会在图上叠成一片且零信息量）；
  context 数值改为 4 位有效数字（`atr14=47.1104` → `47.11`）；
  选中信号后图表视野夹到数据末端（原本右半屏全是空白）；
  单级别规则的层级显示去掉冗余的角色名；补内联 favicon（消掉控制台 404）。
  这些都是**截图看出来的** —— 新增 `scripts/shoot_panel.mjs` 做面板视觉自检。
- `RuntimeStore` 改为 `check_same_thread=False` + 锁串行化：Web 与引擎同进程时
  FastAPI 会把同步端点丢进线程池，而 sqlite3 连接默认绑定创建它的线程。
- `watch.py --web` 退出时优雅关停 uvicorn（`should_exit`），不再每次正常退出都打一段
  CancelledError 回溯 —— 那会训练人忽略真正的报错。
- `pyproject.toml` 声明 `package-data`，否则正式打包会漏掉面板的全部静态资源。
- `BarBuilder` 现在让**恰好落在桶边界的那根 1m 当场收盘**（墙钟分桶下它就是本桶最后一根），
  而不是等下一根来触发桶切换。不改的话每条多级别规则都会晚一根，
  连 `role_bars` 与去重键都会错位。
- 冷却曾把"解冷那一根"消耗掉（只解冷不判定），导致实际冷却比配置多一根 bar，
  且多出的时长随周期变化。现在解冷那一根继续参与判定，冷却精确等于配置秒数。
- 规则加载兼容 YAML 1.1 把裸键 `on:` 解析成布尔 `True` 的行为
  （M1 时因为有 `timeframe:` 兜底而未暴露）。

### Changed
- **买卖点改为圆点 + `B`/`S` 字母**，不再用箭头。箭头只有朝向之分，
  一屏全是多头信号时看着就是一模一样的一片。中性信号**不写字母** ——
  它既不是买也不是卖，硬安一个字母是撒谎。
- **图上三种形状各司其职**：圆点 = 信号、方块 = 开仓成交、箭头 = 离场成交
  （朝向 = 平仓方向）。信号改成圆点后，离场原本也是圆点、同色时完全分不开。

### Fixed
- **跨零点夜盘的凌晨段，跨周末时交易日归错**（`MarketCalendar.trading_day`）：
  周五夜盘 23:59 归下周一、同一场的周六 00:00 却归周六 —— **一场夜盘被劈成两个交易日**。
  后果是日线聚合抛「时间倒流」、Parquet 分到两个分区；接上 au2610（夜盘到 02:30）后
  `watch.py` 直接崩在启动阶段。`in_session` 一直判对了，只有 `trading_day` 没跟上 ——
  两个本该同源的判定各写各的。**修完历史数据必须重补。**
- **`backfill.py` 跨度大就读超时**（实测两个月必挂、一周稳过），且超时抛在流读取阶段、
  看起来像"连不上"。改为内部按周分段拉（幂等）。
- **期货历史按标的降级**：5 个品种里 1 个拉失败，原先整个期货市场被跳过。
  现在只丢拉不动的那个。
- **推翻原设计**：不再用数据源的 8888 主力连续做回测（两接口口径不一致，
  272/345 根 1m 数据不同）。改为一切以真实合约为准，连续序列自行拼接。
- 多周期分桶由"按 session 内累计"改为实测确认的"纯墙钟对齐 + 跳过空桶"。
- M2 的多级别规则样板从 `config/rules/` 移到 `docs/examples/m2-trend-pullback.yaml` ——
  `config/rules/` 下的文件会被真加载，放一个当前加载不了的样板会让整个启动失败。
- **加密交易所由 Binance 改为 OKX**（ADR-0005）：实测 Binance 合约 WS（fstream）在开发机
  握手成功但一帧数据都不来（kline/aggTrade 两次复现），而同机 Binance 现货 WS 正常 ——
  网络侧限制。OKX 永续 WS+REST 全通，且 `confirm` 字段让收盘判定不依赖本地时钟。
  `config/symbols.yaml` 中 `CRYPTO.BINANCE.BTCUSDT.PERP` 相应替换为 `CRYPTO.OKX.BTCUSDT.PERP`。

### Security
- Quote API 自签名证书改用**证书指纹固定**，不使用官方示例建议的 `verify=False`
  （请求头携带 AK，关闭校验等于放弃 MITM 防护）。默认拒绝未配置指纹的连接。
- `feed/polling`：期货实时轮询 Feed —— 重叠窗口去重（`BarCursor`）、缺口检测、
  非交易时段不轮询、拉取时机对齐到「bar 收盘 + 1.5s」
